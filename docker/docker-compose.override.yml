version: "3.9"

services:

  product-service:
    build:
      context: ../services/ProductService
    ports:
      - "5001:8080"
    environment:
      ConnectionStrings__DefaultConnection: "Server=sqlserver;Database=ProductDb;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True"
    depends_on:
      - sqlserver
    networks:
      - backend

  # order-service:
  #   build:
  #     context: ../src/OrderService
  #   ports:
  #     - "5002:8080"
  #   environment:
  #     ConnectionStrings__DefaultConnection: "Server=sqlserver;Database=OrderDb;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True"
  #   depends_on:
  #     - sqlserver
  #   networks:
  #     - backend

  # identity-service:
  #   build:
  #     context: ../src/IdentityService
  #   ports:
  #     - "5003:8080"
  #   environment:
  #     ConnectionStrings__DefaultConnection: "Server=sqlserver;Database=IdentityDb;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True"
  #   depends_on:
  #     - sqlserver
  #   networks:
  #     - backend